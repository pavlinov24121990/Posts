<%= render 'shared/errors', object: @comment %>
 <%= form_with model: [@admin, @post, @comment] do |c| %>
  <%= c.label :body, 'body' %>
  <%= c.text_field :body %>

  <!-- Кнопка отправки формы -->
  <%= c.submit 'Отправить' %>
<% end %>


